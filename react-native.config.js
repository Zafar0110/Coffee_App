////this is use for both running app android and ios then
// run " npx react-native-asset " this command for enclude all assets

module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./src/assets'],
};
